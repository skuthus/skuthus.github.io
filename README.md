# skuthus.github.io

Shortlinks.

- [`/e2e`](https://skuthus.github.io/e2e) — Omarchy Mac volunteer E2E capture

New install, Asahi as root, after `nmtui`:

```bash
curl -fsSL https://skuthus.github.io/e2e | bash
```

Already installed — this is the report you share:

```bash
sudo bash -c 'curl -fsSL https://skuthus.github.io/e2e | bash -s -- final'
```

Attach `~/omarchy-mac-e2e/REPORT.md` after that command finishes.

Full instructions: https://github.com/skuthus/omarchy-mac-e2e-test-capture
